%h2 Interest
- if @customer.account.interest == 0
  %h2.text-danger 0%
- else
  %h2.text-info= "#{@customer.account.interest}%"
  %p
    Next on:
    %br
    %span.glyphicon.glyphicon-calendar
    %strong
      = "#{Date::DAYNAMES[Date.today.next_month.beginning_of_month.wday]},"
      = Date.today.next_month.beginning_of_month
= link_to edit_customer_account_path(@customer), class: 'btn btn-primary' do
  %span.glyphicon.glyphicon-cog
  = ' Account settings'